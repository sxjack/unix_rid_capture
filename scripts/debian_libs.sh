#!/bin/sh
#
# I can't remember how I installed perl IO::Socket, probably 'cpan IO::Socket'.
# I used cpan to install all the perl modules on my mac -
# cpan Curses IO::Socket JSON
#

/usr/bin/apt install libncurses-dev libpcap-dev libgcrypt20 bluez

/usr/bin/apt install libcurses-perl libjson-perl


